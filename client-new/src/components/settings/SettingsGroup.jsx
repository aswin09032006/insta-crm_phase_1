import React from 'react';

const SettingsGroup = ({ children }) => {
 return (
 <div className="card-panel overflow-hidden transition-all duration-200">
 {children}
 </div>
 );
};

export default SettingsGroup;
